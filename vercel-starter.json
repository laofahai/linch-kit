{
  "version": 2,
  "installCommand": "bun install",
  "buildCommand": "bun run build:packages && turbo run build --filter=@linch-kit/starter",
  "outputDirectory": "apps/starter/.next",
  "framework": "nextjs",
  "env": {
    "NODE_ENV": "production"
  }
}
