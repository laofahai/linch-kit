# Getting Started

This guide will help you create your first Linch Kit application in just a few minutes.

## Prerequisites

Before you begin, make sure you have the following installed:

- **Node.js** 18.0 or later
- **npm**, **yarn**, or **pnpm** package manager
- **Git** for version control

## Installation

### Create a New Project

The fastest way to get started is using our CLI tool:

```bash
npx create-linch-app my-app
```

This command will:
- Create a new directory with your project name
- Install all necessary dependencies
- Set up the basic project structure
- Configure development tools

### Navigate to Your Project

```bash
cd my-app
```

### Start the Development Server

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
```

Open [http://localhost:3000](http://localhost:3000) in your browser to see your application running.

## Your First Entity

Let's create your first data entity:

```typescript
import { z } from 'zod'
import { defineEntity, primary, unique, createdAt, updatedAt } from '@linch-kit/schema'

export const User = defineEntity('User', {
  id: primary(z.string().uuid()),
  email: unique(z.string().email()),
  name: z.string(),
  role: z.enum(['admin', 'user']).default('user'),
  createdAt: createdAt(z.date()),
  updatedAt: updatedAt(z.date()),
})
```

## Generate Database Schema

Run the following command to generate your database schema:

```bash
npx linch schema:generate
```

## Next Steps

Congratulations! You now have a working Linch Kit application. Here's what you can do next:

- [Learn Core Concepts](/docs/guides/core-concepts) - Understand how Linch Kit works
- [Explore Plugins](/docs/guides/plugins) - Add functionality with plugins
- [Customize UI](/docs/guides/ui-customization) - Customize the look and feel
- [Deploy Your App](/docs/guides/deployment) - Deploy to production
