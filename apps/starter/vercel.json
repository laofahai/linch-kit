{
  "buildCommand": "cd ../.. && bun install && bun run build --filter=starter",
  "devCommand": "bun run dev",
  "installCommand": "bun install",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "env": {
    "NODE_ENV": "production"
  }
}
