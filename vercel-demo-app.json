{
  "version": 2,
  "installCommand": "bun install",
  "buildCommand": "bun run build:packages && turbo run build --filter=demo-app",
  "outputDirectory": "apps/demo-app/.next",
  "framework": "nextjs",
  "env": {
    "NODE_ENV": "production"
  }
}
